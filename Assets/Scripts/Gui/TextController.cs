using UnityEngine;

public class TextController : MonoBehaviour
{
    [Tooltip("Minimum")] public float min = 4f;
    [Tooltip("Maximum")] public float max = 5f;
    [Tooltip("Change / Second")] public float velocity = 1f;

    void Start()
    {
    }

    
    private void OnMouseEnter()
    {
    }

    private void OnMouseExit()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}